---
name: 3rdpartybaselibs-orchestrator-recipe
description: >-
  Manages, audits, and maintains the 3rdPartyBaseLibs build recipe for third-party
  C/C++ base libraries powered by csOrchestrator. Use when inspecting or updating
  the base-libraries build pipeline, validating recipe consistency (extra files,
  versions, and repositories), re-generating the GitHub Actions CI workflow, or
  running pre-commit and quality checks.
---

# 3rdPartyBaseLibs Orchestrator Recipe Management

## Overview

This skill guides the agent in inspecting, auditing consistency, updating, and maintaining the **3rdPartyBaseLibs** build recipe. `3rdPartyBaseLibs` uses the `csOrchestrator` framework to define cross-platform third-party base-library build pipelines in Python and generate corresponding GitHub Actions CI workflows.

## Dependencies

- **Python**: `>= 3.11` in active `.venv`
- **csOrchestrator**: Available as an editable package in the local environment
- **Tools**: `pre-commit`, `ruff`, `mypy`, `pytest`

## Quick Start

To audit recipe consistency and re-generate the GitHub Actions CI workflow:

```bash
# 1. Re-generate GitHub Actions workflow from 3rdPartyBaseLibs.py
./3rdPartyBaseLibs.py generate-github-workflow

# 2. Run quality checks and ensure git hygiene
pre-commit run --all-files
```

## Workflow

Follow these steps when interacting with or modifying the `3rdPartyBaseLibs` build recipe:

### 1. Recipe & Consistency Audit
Inspect the core configuration files before making changes or upon request:
- [`3rdPartyBaseLibs.py`](file:///home/sceriani/myWorkspace/3rdPartyBaseLibs/3rdPartyBaseLibs.py): Core pipeline definition, version, and repository list.
- [`pyproject.toml`](file:///home/sceriani/myWorkspace/3rdPartyBaseLibs/pyproject.toml): Project metadata and versioning.
- [`3rdPartyBaseLibs/cs_orchestrator_config.py`](file:///home/sceriani/myWorkspace/3rdPartyBaseLibs/3rdPartyBaseLibs/cs_orchestrator_config.py): Compiler and toolchain mappings (currently a placeholder).
- [`tests/`](file:///home/sceriani/myWorkspace/3rdPartyBaseLibs/tests): Placeholder tests; real recipe tests are added later.

**Verification Checklist (Strict Fail-Loud Policy):**
- **Extra Files**: Check every path passed to `additional_files_list` in `create_default_orchestrator()` (e.g. `Path("3rdPartyBaseLibs/cs_orchestrator_config.py")`). Verify that every referenced file exists on disk.
- **Version Alignment**: Verify that `version` in `3rdPartyBaseLibs.py` (e.g. `0.1.0`) aligns with `version` in `pyproject.toml` and artifact package names (`3rdPartyBaseLibs-<version>-...`).
- **Repository List**: Verify that every repository in `repos` is a known csOrchestrator-supported repo and that its `BuildConfig` (`RELEASE`, `DEBUG_RELEASE`, or `None` for checkout-only repos like `csCMake`) matches the repository's intended configuration.
- **SDK Vendoring**: `csorchestratorsdk/portable/` is autogenerated from `csOrchestrator`; do not edit manually. If it needs updating, re-export it from `csOrchestrator`.

> [!IMPORTANT]
> If any consistency check fails (missing extra file, mismatched version, unknown repository), **fail loudly**. Report the exact discrepancies to the user for guidance rather than silently continuing or guessing.

### 2. Local Execution Policy (Build Guard)
- **DO NOT run full local builds of all base libraries** (e.g., executing all phases of `./3rdPartyBaseLibs.py` locally). Building every library on the full matrix requires significant time, disk space, and RAM.
- Only run unit tests (`pytest`) or dry-run validation when checking local configuration logic.

### 3. CI Workflow Re-generation
Whenever `3rdPartyBaseLibs.py`, the repository list, or execution phases are modified, re-generate the GitHub Actions workflow:

```bash
./3rdPartyBaseLibs.py generate-github-workflow
```

- Verify that [`.github/workflows/3rdPartyBaseLibs.yml`](file:///home/sceriani/myWorkspace/3rdPartyBaseLibs/.github/workflows/3rdPartyBaseLibs.yml) has been updated with the modified phases and matrix.
- Inspect the git diff using `git diff .github/workflows/3rdPartyBaseLibs.yml` to ensure changes match expectations.

### 4. Code Quality & Pre-commit Verification
Before concluding any changes, run the repository's validation hooks:

```bash
pre-commit run --all-files
```

If hooks report formatting errors or auto-fix lines:
1. Review the modified files.
2. Re-run `pre-commit run --all-files` to ensure all checks pass cleanly (`Passed`).

### 5. Documentation Synchronization
If recipe arguments, version, or repository configurations were updated:
- Update [`README.md`](file:///home/sceriani/myWorkspace/3rdPartyBaseLibs/README.md) to keep the included-libraries table, build matrix, and command instructions accurate.

## Rate Limiting

Not applicable. This skill operates entirely on local filesystem artifacts, repository tools, and git workflows.

## Common Mistakes

1. **Attempting a full local build**: Running `./3rdPartyBaseLibs.py` without understanding that it will attempt to download and build all base libraries. Full builds are meant for CI runners or dedicated build machines.
2. **Forgetting to re-generate the workflow**: Modifying `3rdPartyBaseLibs.py` without running `./3rdPartyBaseLibs.py generate-github-workflow`, leaving `.github/workflows/3rdPartyBaseLibs.yml` out of sync with the Python recipe.
3. **Missing extra files in release**: Adding a file to `additional_files_list` in `3rdPartyBaseLibs.py` that does not exist or has a typo in its relative path, causing release packaging jobs to fail in CI.
4. **Version mismatch**: Updating `version` in `3rdPartyBaseLibs.py` without updating `pyproject.toml` or the artifact names (`3rdPartyBaseLibs-<version>-...`).
